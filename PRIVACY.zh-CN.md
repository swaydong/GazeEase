# GazeEase 隐私说明

[English](PRIVACY.md) · 简体中文

最后更新：2026 年 8 月 12 日

## 摘要

GazeEase 是一个只在本机运行的 macOS 护眼计时器。它判断电脑是否正在被使用、估算疲劳、验证休息并在本机提醒。App 不包含账号系统、广告、遥测、云同步或网络上传路径。

本文描述当前 Bundle ID 为 `com.local.EyeProtection` 的私有本机版本。

## 使用的权限

GazeEase 申请“输入监控”权限，用来检测键盘、点击、滚动、触控板和鼠标移动。该权限仅用于：

- 累积有效使用时间。
- 开启可选的“长时间无操作自动休息”后，判断无操作时长。
- 活动恢复时中断手动休息。

App 可能在内存中即时判断鼠标移动是否足够明显，但不会保存鼠标坐标。

GazeEase **不申请“屏幕录制”权限**，不会采集、检查、保存或传输屏幕画面。

## 本地保存的数据

GazeEase 只保存计时、提醒、重启恢复和“今日 / 近 7 日”本地分析所需要的信息：

- 输入活动类型与时间，不包含按键内容。
- 疲劳采样与有效使用时长。
- 过载过程及提醒响应时长。
- 休息尝试的开始/结束时间、来源、结果与中断原因。
- 每日汇总指标。
- 使用/休息时长、提醒方式、皮肤、随机换肤计划和无操作休息等偏好。登录启动状态直接向 macOS 查询，不会复制到分析数据库。
- App 重启后恢复疲劳和未完成休息要求所需的运行状态。

当前本地存储位置包括：

```text
~/Library/Preferences/com.local.EyeProtection.plist
~/Library/Application Support/EyeProtection.store
~/Library/Application Support/EyeProtection.store-shm
~/Library/Application Support/EyeProtection.store-wal
```

## 不保存、不收集的数据

GazeEase 不保存或收集：

- 按键内容或输入的文字。
- 鼠标坐标。
- 截图、录屏或屏幕像素。
- 窗口标题或文档内容。
- 音频或麦克风数据。
- 剪贴板内容。
- 浏览记录。
- 账号、联系人、精确位置、支付、广告或设备指纹数据。

App 不包含第三方分析或广告 SDK，不会把个人数据上传给开发者或其他服务。

## 保留期限

当前自动保留期限为：

- 疲劳采样：7 天。
- 过载过程与休息尝试明细：30 天。
- 每日汇总：365 天。

GazeEase 启动维护流程时，会在本机移除超过期限的记录。

## 用户控制与删除

设置窗口提供“清除所有分析数据”，用于清除本地分析记录。使用 `Uninstall GazeEase.command` 卸载时会刻意保留偏好和历史，方便以后重装继续使用。

如需彻底移除，请先退出并卸载 GazeEase，再按[安装文档](INSTALL.zh-CN.md)中的明确步骤清理本地数据。GazeEase 脚本不会运行 `tccutil`、重置系统隐私数据库或静默删除历史。

输入监控权限始终由 macOS 管理，可在“系统设置 → 隐私与安全性 → 输入监控”中手动移除。

## 本地通知与系统状态

休息通知通过 macOS 在本机生成。GazeEase 还会观察本机锁屏、显示器休眠和系统睡眠状态，使足够长的系统休息可以完成已设置的休息时长。这些信号不会上传。

## 分发限制

当前安装包是供本机使用的私有 Apple Development 签名版本。未来公开发布时，应以最终 Bundle ID、分发渠道和支持联系方式为基础同步更新隐私说明。
